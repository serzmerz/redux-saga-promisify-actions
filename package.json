{
  "name": "redux-saga-promisify-actions",
  "version": "1.0.0",
  "description": "Make promise from your action creator for use it in your components",
  "main": "lib/index.js",
  "repository": "serzmerz/redux-saga-promisify-actions",
  "scripts": {
    "build": "babel src -d lib",
    "lint": "standard src/*.js tests/*.js --fix",
    "test": "jest",
    "prepublish": "npm run build"
  },
  "keywords": [
    "redux-saga",
    "redux",
    "promisify",
    "action"
  ],
  "author": "Sergey Tansky",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.2.3",
    "@babel/core": "^7.2.2",
    "@babel/plugin-transform-runtime": "^7.2.0",
    "@babel/preset-env": "^7.3.1",
    "@babel/runtime": "^7.3.1",
    "babel-preset-minify": "^0.5.0",
    "jest": "^24.0.0",
    "redux-saga": ">=0.15.0",
    "standard": "^12.0.1"
  },
  "peerDependencies": {
    "@babel/runtime": ">=7.0.0",
    "redux-saga": ">=0.15.0"
  },
  "standard": {
    "globals": [
      "jest",
      "describe",
      "expect",
      "beforeEach",
      "test"
    ]
  }
}
